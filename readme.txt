INDEX
El index va a requerir 
PLANTILLA adminLTE

nuestro sistema se va a dividir por módulos

URL´s amigables o lista blanca


HTACCESS
en la primer opción ocultamos todas las carpetas que tiene el proyecto
para la URL´s amigables habilitamos la reescritura con la segunda opción en Engine de la escritura
y la regla de reescritura va dentro de una expresión regular dentro de los parentesis colocamos corchetes 
donde en su interior van los caracteres que vamos a permitir en las URL's y el simbolo más. Nota este archivo NO
debe llevar nada más que el código, no puede llevar comentarios porque no funciona.

LOGIN
Para separar el contenido del sistema, de cuando el usuario NO esté logueado a cuando este logueado vamos a utilizar dentro
de las etiquetas PHP, una estructura condicional (if) y vamos a preguntar si no existe una variable de sesión cualquiera
entonces el usuario no esta logueado debe loguearse

FUNCIONALIDADES
Son funciones que realiza el sistema 

CASOS DE USO
Cada caso de uso comienza con un formulario, que puede ir alojado o  no, en una ventana modal, esta ventana se ejecuta mediante un botón 

FORMULARIO
En la programación orientada a objetos los formularios no tienen el atributo action, sino que recibe los datos mediante
el atributo method, y las cajas de texto o input´s deben llevar un atributo de tipo name que va a identificar a cada input,


para intervenir los estilos de la página de login nos vamos al archivo css en la fila 34972

TABLAS
Utilizamos el plugin dataTable
